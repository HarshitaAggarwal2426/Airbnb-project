<style>
  .search-btn {
    background-color: #fe424d;
    color: #fff;
    border-radius: 25px;
    padding: 0 1rem 0 1rem;
  }
  .search-btn:hover {
    background-color: #fe424d;
    color: #fff;
  }

  .search-btn i {
    display: inline;
    margin-right: 0.5rem;
  }

  .search-inp {
    border-radius: 25px;
    padding: 0.5rem 3rem 0.5rem 3rem;
    font-size: 0.8;
  }
</style>

<nav class="navbar navbar-expand-md bg-body-light border-bottom sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="/listings"
      ><i class="fa-regular fa-compass"></i
    ></a>
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNavAltMarkup"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
      <div class="navbar-nav">
        <a class="nav-link" href="/listings">Explore</a>
      </div>
      <!-- <div class="navbar-nav ms-auto">
        <form class="d-flex" role="search">
          <input
            class="form-control me-2 search-inp"
            type="search"
            placeholder="Search destinations"
          />
          <button class="btn search-btn" type="submit">
            <i class="fa-solid fa-magnifying-glass"></i>Search
          </button>
        </form>
      </div> -->
      <div class="airbnb-search ms-auto">
        <div class="airbnb-field" id="whereField">
          <label>Where</label>
          <span id="selected-destination">Search destinations</span>

          <!-- Dropdown -->
          <div class="dropdown" id="whereDropdown">
            <ul>
              <li>Trending</li>
              <li>Rooms</li>
              <li>Iconic Cities</li>
              <li>Mountains</li>
              <li>Castles</li>
              <li>Amazing Pools</li>
              <li>Camping</li>
              <li>Farms</li>
              <li>Arctic</li>
              <li>Domes</li>
              <li>Boats</li>
            </ul>
          </div>
        </div>

        <div class="airbnb-field">
          <label>Check in</label>
          <input type="date" id="checkin" />
        </div>

        <div class="airbnb-field">
          <label>Check out</label>
          <input type="date" id="checkout" />
        </div>

        <div class="airbnb-field">
          <label>Who</label>
          <input
            type="number"
            id="guests"
            placeholder="Add guests"
            min="1"
            max="10"
          />
        </div>

        <button class="search-round-btn" id="searchBtn">
          <i class="fa-solid fa-magnifying-glass"></i>
        </button>
      </div>

      <div class="navbar-nav ms-auto">
        <a class="nav-link" href="/listings/new">Airbnb your home</a>
        <% if (typeof currUser === "undefined" || !currUser) { %>

        <a class="nav-link" href="/signup"><b>Signup</b></a>
        <a class="nav-link" href="/login"><b>Login</b></a>
        <% } else{ %>
        <a class="nav-link" href="/logout"><b>Logout</b></a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
